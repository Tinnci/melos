{
    "name": "melos-monorepo",
    "version": "0.0.1",
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "bun-types": "^1.3.3",
        "typescript": "^5.0.0"
    },
    "scripts": {
        "build": "bun run --filter '*' build",
        "test": "bun test"
    }
}